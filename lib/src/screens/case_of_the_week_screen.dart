
import 'package:flutter/material.dart';

class CaseOfTheWeekScreen extends StatelessWidget {
  const CaseOfTheWeekScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Case of the Week'),
      ),
      body: const Center(
        child: Text('Case of the Week Screen - Coming Soon!'),
      ),
    );
  }
}
